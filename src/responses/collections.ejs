[
<% for(var i=0; i < collections.length; i++) { col=collections[i].collection; links=col.links; items=col.items, itemLinks=items.links, itemData=items.data; %> {
   "collection": {
      "version": "<%= col.version %>",
      "href": "<%= col.href %>",
      "links": [<% for(var x=0; x < links.length; x++) { %>
        { "rel": "<%= links[x].rel %>", "name": "<%= links[x].context %>", "prompt": "<%= links[x].prompt %>", "title": "<%= links[x].title %>",
          "href": "<%= links[x].href %>"<% if (links[x].render) { %>, "render": "<%= links[x].render %>"<% } %>
        }<% if (x < (links.length - 1)) { %>,<% } %>
     <% } %>],
     "items": [<% for(var z=0; z < items.length; z++) { %>
       { "href": "<%= items[z].href %>"<% if (items[z].render) { %>, "render": "<%= items[z].render %>"<% } %>
       
       }<% if (z < (items.length - 1)) { %>,<% } %>
    <% } %>] 

   }
 }<% if(i < (collections.length - 1)) { %>,<% } %>
<% } %>]
